const DeleteGenreForm = ({ rowObject, backendURL, refreshGenres }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteGenreForm;